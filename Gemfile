# frozen_string_literal: true

# Only the specs for the Git helper scripts in private_dot_config/git/bin.
# Nothing chezmoi applies depends on this file.

source 'https://rubygems.org'

gem 'rspec', '~> 3.13'
