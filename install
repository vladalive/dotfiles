#!/usr/bin/env bash

set -e

BASEDIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

if ! command -v chezmoi >/dev/null 2>&1; then
  echo "chezmoi is required. Install it first: https://www.chezmoi.io/install/" >&2
  exit 1
fi

exec chezmoi --source "${BASEDIR}" --force apply "${@}"
